services:
  serphouse-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: serphouse-mcp:latest
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      HOST: ${HOST:-0.0.0.0}
      PORT: ${PORT:-3000}
    restart: unless-stopped
